# SITREP PRD — AI Plays Pokémon (ai-plays-poke) — E2E Tick T82

**Project:** PTP-01X — Autonomous Pokémon (Gen 1) Gameplay AI Benchmark
**SITREP Date:** 2026-08-04 | **Tick:** T82 (E2E-001 fixture, FIRST tick of due window T82–T87)
**Status:** 🟢 ALL FIXES HOLD — starter picked at c16 (fastest yet); leak fix flat 108MB; HSM spam 0; no new findings
**Repo:** `/home/kara/ai_plays_poke` | **HEAD:** `bcdf8ea` (T81, pre-tick)

---

## 1. EXECUTIVE SUMMARY

E2E-001 executed at the first tick of its due window (T82–T87) with 1 live
Luna-controller run (`cron_runner.py`, `openai/gpt-5.6-luna` via OpenRouter,
80 cycles) under the RSS watchdog (kill > 2.5 GB, 10s /proc poll).

**✅ Starter pick — FASTEST YET (cycle 16).** Oak's Lab menu detected at
cycle 16, first starter selected deterministically, `party_count=1`,
species Charmander, nickname declined. T77: c14/c22; T72: c18–21; T68: c18.
The same-tile stuck detector fired correctly first (tile-locked map 40
@(5,3) x8 → `starter_approach` L0 recovery, attempt 2/5) before the
deterministic menu branch took over — the two-layer design works as intended.

**✅ Leak fix HOLDS — 108 MB peak flat.** Full 80/80 cycles at
107→108 MB RSS, zero growth slope. T72's identical pipeline died at
~50 GB in ~9 min. (No battle encounter this run — the leak trigger path
was battle-phase; prior live battle verification stands: T72 5-run suite,
T77 14-battle-cycle run at 108 MB.)

**✅ HSM transition spam — 0.** Zero `Invalid transition attempted`
messages (T72: infinite spam). Alias-aware transition fix holds.

**✅ Recovery ladder healthy — 12 events, all bounded.** 7×
`dialog_advance` (screen-locked dialog, attempts bounded 2-3/5), 5×
Level-0 (starter_approach tile-locked). No un-bounded loops, no escape
attempts needed.

**ℹ️ No battle encounter this run** — GAMEPLAY-ESCAPE-001's fix
(trainer-flag 0xD057, menu-actionability gates, flee bound) was verified
live at T78 (run2 t78_flee_verify2: 0 futile flee calls, battle-aware
recovery L0/L1 bounded) and is not re-exercised here. No regression
signal in recovery behavior.

**No new tasks.** All standing fixes verified. Board stays: E2E-001
(recurring, next due T87), NEVER-DONE (perpetual audit).

---

## 2. RUN LOG (2026-08-04, `luna_v13_*`)

| Run | Cycles | Starter | Battle | End | Peak RSS | Cost (API) |
|---|---|---|---|---|---|---|
| 1 (04:00Z) | 80 (full) | ✅ c16 Charmander | none | natural, exit 0 | **108 MB** | $0.52 |

Run detail: screens — overworld 38, dialog 33, starter_selection 1 (+ 8
intro/other); map 40 (Oak's Lab) x7; events — recovery ×12 (7
dialog_advance, 5 Level-0), starter_selection c16 + starter_picked c16,
state_saved checkpoints throughout; HSM spam: **0**; flee calls: 0 (no
battles). API: 38 calls, 93,811 in / 3,531 out tokens, all Success: True,
~$0.0145/call. Wall time ~3.5 min (04:00:04 → 04:03:24) — RAM-reader
pipeline + frame cache keep cycle cost low.

Video: `videos/luna_v13_20260804_0400.mp4` (672 KB, 80 frames @ 2fps, 40s).

---

## 3. AUDIT (NEVER-DONE 11-point, same tick)

| Gate | Result |
|---|---|
| gitreins guard | 4/5 — static_analysis FAIL = **standing T81 tooling drift** (gitreins 0.8.2 wheel hardcodes `--strict` in mypy, overrides mypy.ini `disallow_untyped_defs=False`; fails on test-file no-untyped-def class). Engine source verified (static_analysis.py:345). NOT a repo regression. |
| mypy src/ | ✅ 0/61 (project scope, per mypy.ini) |
| ruff | ✅ PASS |
| compileall | ✅ PASS |
| TODO/FIXME (src/ + cron_runner.py) | ✅ 0 |
| pytest collect | 3860 (T81 baseline; full suite 3847 pass/8 skip/5 deselect at T78) |
| CI (gh run list) | ✅ green 5/5 (last 30884759969 = T81 push, 3m17s) |
| gitreins tasks | ✅ 6/6 complete, 0 pending |
| Issues / unpushed / incoming | ✅ 0 / 0 / 0 |
| Scheduler | ✅ Enabled, CooldownS=7200 pinned (fleet.toml matches API — no PUT) |
| Deps | minor churn standing (aiohttp, anthropic) — no action; pip-audit skipped per project scope |

---

## 4. OFF-BY-ONE & DUCKBRAIN

- Off-by-one: sub_0d86be (T81, drift class) instant-failed server-side →
  resubmitted ONCE as sub_b9e43e (queued, pos 3). Prior sub_04a38a /
  sub_056278 also instant-failed — server solve path flaky; protocol
  honored (resubmit once, stop on repeat). Discover
  `python-emulator-gameplay-audit` → not_found.
- DuckBrain: `/project/ai-plays-poke/status` + `/ticks/t82-e2e` written.

---

## 5. NEXT TICK (T83)

NEVER-DONE audit (E2E-001 next due T87). Standing drift flagged upstream
(gitreins-poc `--strict` opt-in). No worker spawn needed.
