# SITREP PRD — AI Plays Pokémon (ai-plays-poke) — E2E Tick T87

**Project:** PTP-01X — Autonomous Pokémon (Gen 1) Gameplay AI Benchmark
**SITREP Date:** 2026-08-04 | **Tick:** T87 (E2E-001 fixture, LAST tick of due window T82–T87)
**Status:** 🟢 ALL FIXES HOLD — TRAINER BATTLE re-exercised live: 0 flee attempts, leak flat 108MB through 28 battle cycles; no new findings
**Repo:** `/home/kara/ai_plays_poke` | **HEAD (pre-tick):** `0f84901` (T86)

---

## 1. EXECUTIVE SUMMARY

E2E-001 executed at the LAST tick of its due window (T82–T87) with 1 live
Luna-controller run (`cron_runner.py`, `openai/gpt-5.6-luna` + deepseek-v4-flash
via OpenRouter, 80 cycles) under the RSS watchdog (kill > 2.5 GB, 10s /proc poll).

**⚔️ TRAINER BATTLE ENCOUNTERED (cycle 52 → 80) — the most valuable E2E since T78.**
The run left Pallet Town, walked into a trainer battle, and the controller fought
it for ~28 cycles. This re-exercised BOTH battle-phase fixes live:

- **GAMEPLAY-ESCAPE-001 (battle-escape loop) — HOLDS, re-proven live.** The
  trainer was correctly classified as TRAINER (wIsInBattle 0xD057 fix — T77's
  failure mode misread 0xD05A and called it wild). **0 futile flee calls**
  (`failed_flee_attempts=0` across every cycle; T77 failure: 14x). The
  controller chose to fight: `select_move(1)` re-issued every cycle with
  battle-aware recovery L0→L4 escalating, bounded at 5/5 attempts. No unbounded
  loop, no spam.
- **GAMEPLAY-LEAK-001 (battle-phase native leak) — HOLDS under REAL battle
  load.** RSS flat 107→108 MB across the entire run INCLUDING ~28 consecutive
  battle cycles (the exact StateWindow loop that killed T72 at ~50 GB/9 min).
  This is the strongest battle-path verification yet — T82 had no battle, T77's
  battle was 14 cycles, and the T72 control died in this loop.

**✅ Starter pick — c38, Charmander, party 0→1.** Oak's Lab menu detected at
cycle 38; tile-locked detector fired first (map 40 @ (5,3) x8 →
`starter_approach` L0, attempt 1/5), then the deterministic menu branch picked
the first starter; nickname declined.

**✅ HSM transition spam — 0.** Zero `Invalid transition attempted` messages.

**✅ Recovery ladder healthy — 10 events, all bounded.** 1× L0
alternate_direction, 1× L0 starter_approach, 5× battle_select_move
(one per ladder level L0→L4, attempts bounded 1-5/5), plus bounded dialog
advances. No un-bounded loops.

**ℹ️ Observation (non-blocking):** the trainer battle did not conclude before
the 80-cycle cap (controller kept selecting move 1). Not a regression — no
loop, no spam, bounded recovery, deliberate fight strategy. Battle
win/loss resolution remains unobserved in the 80-cycle envelope.

**No new tasks.** All standing fixes verified with the strongest evidence to
date. Board stays: E2E-001 (recurring, next due ~T92-T97), NEVER-DONE
(perpetual audit).

---

## 2. RUN LOG (2026-08-04, `luna_v14_*`)

| Run | Cycles | Starter | Battle | End | Peak RSS | Cost (API) |
|---|---|---|---|---|---|---|
| 1 (17:24Z) | 80 (full) | ✅ c38 Charmander | ⚔️ trainer, c52→80 | natural, exit 0 | **108 MB** | $0.91 |

Run detail: 92 actions; screens — battle, overworld, menu, dialog; maps —
Pallet Town (map 0) → Oak's Lab (map 40) → Route 1 area (trainer battle);
events — 10 recovery (1 alternate_direction, 1 starter_approach, 5
battle_select_move ladder L0-L4, 3 dialog), 35 state changes, 9 checkpoints
(slots 1-2); HSM spam: **0**; futile flee attempts: **0**; frame cache 192
unique / 397 refs. API: 58 calls (Luna for early game, deepseek-v4-flash for
battle), 100% Success, ~$0.91 total, wall ~5.4 min (17:24:46 → 17:30:0x).

Video: `videos/luna_v14_20260804_1724.mp4`.

---

## 3. AUDIT (NEVER-DONE 11-point, same tick)

| Gate | Result |
|---|---|
| gitreins guard | ✅ 5/5 PASS full mode (secrets/lint/tests/static_analysis/lsp) — gitreins 0.11.0; T81-T83 `--strict` drift resolution holds (T84+ confirmed) |
| mypy src/ | ✅ 0/61 (project scope, per mypy.ini) |
| ruff | ✅ PASS |
| compileall | ✅ PASS |
| TODO/FIXME (src/ + cron_runner.py) | ✅ 0 |
| pytest collect | 3860 (T81 baseline; full suite 3847 pass/8 skip at T78) |
| CI (gh run list) | ✅ green 5/5 (last 30947138972 = T86 push, 3m20s) |
| gitreins tasks | ✅ 6/6 complete, 0 pending |
| Issues / unpushed / incoming | ✅ 0 / 0 / 0 |
| Scheduler | ✅ Enabled, CooldownS=900 enforcer-applied 13:11Z (E2E-001 fixture pending; 11 projects sped up) vs fleet.toml:25 pin 7200 — NO PUT (enforcer re-applies; pin is durable restart value) |
| Deps | minor churn standing (aiohttp, anthropic) — no action; pip-audit skipped per project scope |

---

## 4. OFF-BY-ONE & DUCKBRAIN

- Off-by-one: new submission for the trainer-battle-with-leak-hold problem
  class (python-emulator-battlepath-leak-hold); discover on next tick.
- DuckBrain: `/project/ai-plays-poke/status` + `/ticks/t87-e2e` written.

---

## 5. NEXT TICK (T88)

NEVER-DONE audit. E2E-001 next due ~T92 (cadence 5-10 ticks). Battle
win/loss resolution inside the 80-cycle envelope is the only standing
observation; not taskified (escape fix holds — no re-open without new
negative evidence).
