# SITREP PRD — AI Plays Pokémon (ai-plays-poke) — E2E Tick T77

**Project:** PTP-01X — Autonomous Pokémon (Gen 1) Gameplay AI Benchmark
**SITREP Date:** 2026-08-03 | **Tick:** T77 (E2E-001 fixture, LAST tick of due window T72–T77)
**Status:** 🟢 LEAK FIX VERIFIED — GAMEPLAY-LEAK-001 holds live; battle-escape loop is the new P2 finding
**Repo:** `/home/kara/ai_plays_poke` | **HEAD:** `5c4cd52` (T76, pre-tick)

---

## 1. EXECUTIVE SUMMARY

E2E-001 executed at the last tick of its due window (T72–T77) with 2 live
Luna-controller runs (`cron_runner.py`, `openai/gpt-5.6-luna` via OpenRouter
+ deepseek-v4-flash battle loop), each under an RSS watchdog (kill > 2.5 GB).

**✅ CRITICAL GATE — GAMEPLAY-LEAK-001 fix HOLDS under E2E.**
The P0 battle-phase memory leak (~70–100 MB/s, ~50 GB death at T72) is
GONE. Run 1 survived a full battle sequence (14 battle cycles, 9+
`run_from_battle` tool calls, 29 min) with **RSS flat at 108 MB peak**.
Run 2 completed all 80 cycles (no battle encounter) at **108 MB flat**.
T72's identical pipeline died at ~50 GB in ~9 min. Leak fix: VERIFIED.

**✅ Milestone — Starter picked LIVE again (c14 / c22).**
Run 1 picked Charmander at cycle 14 (faster than T72's 18–21);
run 2 at cycle 22. Party 0→1, nickname declined, no name-entry stall.

**✅ Secondary T72 finding — HSM transition spam GONE.**
Zero `Invalid transition attempted: BOOT.INITIALIZE -> BATTLE.BATTLE_MENU`
messages in either run (T72: infinite spam). Alias-transition fix holds.

**🚨 NEW P2 FINDING — Battle-escape loop: `run_from_battle({})` never lands.**
Run 1 entered a wild battle at cycle 20 and called `run_from_battle({})`
14 times over 18 cycles (~30 min) without the battle resolving. The flee
sequence (FIGHT→right→down→A + wait 60 + FF 180) is either missing the RUN
menu slot or failing repeatedly; the battle StateWindow keeps re-selecting
the same tool with empty args, and the recovery ladder fires (6 recovery
events) but no recovery is battle-aware. Pre-existing pattern (T73 leakfix4
showed the same 10x `run_from_battle` spam) — NOT a regression from the
leak fix. New board task: **GAMEPLAY-ESCAPE-001** (P2).

---

## 2. RUN LOG (2026-08-03, all `luna_v12_*`)

| Run | Cycles | Starter | Battle | End | Peak RSS | Cost (API) |
|---|---|---|---|---|---|---|
| 1 (14:18Z) | 38 | ✅ c14 | c20, 14x flee loop | killed (loop cap) | **108 MB** | ~$3.77 |
| 2 (14:44Z) | 80 (full) | ✅ c22 | none | natural, exit 0 | **108 MB** | ~$0.63 |

Run 1 detail: screens — overworld 7, dialog 8, menu 1, battle 14; actions —
`run_from_battle` 14, `press_button` 8, `confirm_first_starter_then_decline_nickname` 1.
Events: recovery ×6 (c10/13/25/26/31/33/36), starter_selection + starter_picked c14,
battle_start c20, state_saved c20/30. HSM spam: **0**.
Run 2 detail: 80/80 cycles, overworld 46 + dialog 27 + menu 1, starter c22,
7 state_saved checkpoints, recovery ×5 (all overworld/dialog — recovery ladder healthy).

Frame cache: 76→125 unique frames (226 refs) after run 2.

---

## 3. MEMORY VERIFICATION (GAMEPLAY-LEAK-001)

| Metric | T72 (pre-fix) | T77 run 1 | T77 run 2 |
|---|---|---|---|
| Peak RSS | ~50 GB (death) | **108 MB** | **108 MB** |
| Battle cycles survived | 4/4 runs died | 14+ battle cycles | n/a (no battle) |
| Runtime | ~9 min to death | 29 min (capped for loop) | 80 cycles full |
| HSM spam | infinite | 0 | 0 |
| RSS slope | +70–100 MB/s | **0 MB/s** | **0 MB/s** |

---

## 4. BATTLE-ESCAPE FINDING (GAMEPLAY-ESCAPE-001, P2)

**Symptoms:** wild battle at c20; `run_from_battle({})` selected 14 consecutive
times with empty args; flee never lands (battle RAM state persists);
recovery ladder fires (screen-locked/tile-locked) but has no battle-aware
rung, so it cannot break the loop. Same signature as T73 leakfix4
(10x run_from_battle, cycles 43–58) — pre-existing, leak-fix-independent.

**Hypotheses (for worker):**
1. Flee menu navigation misses RUN: Gen 1 battle menu cursor position after
   state-window re-entry may not be at FIGHT (tool assumes FIGHT→right→down).
2. Flee succeeds but battle RAM flag / HSM state persists → state window
   never sees the transition (battle_end event exists in state_window.py:521
   but may not fire on flee).
3. deepseek battle prompt keeps recommending flee because party is a single
   level-5 Charmander vs faster wild — needs move/switch fallback logic.

**Proposed fix scope (worker, gpt-5.6-sol):** add flee-attempt counter +
menu-state verification (RAM read of battle menu cursor), alternate
`select_move` fallback after N flee failures, and battle-aware recovery
rung (press B to reset menu, re-read RAM). Verify: 80-cycle run survives
battle and re-enters overworld.

---

## 5. NEVER-DONE AUDIT (11-point)

| Check | Result |
|---|---|
| git status | ✅ CLEAN (logs/videos/screenshots gitignored) |
| GitReins guard (full) | ✅ PASS 5/5 (1st run transient mypy cache miss on fastapi in py3.14 site-packages; clean re-run, exact cmd EXIT=0) |
| mypy | ✅ 0 errors / 61 files |
| ruff | ✅ PASS |
| compileall | ✅ PASS |
| TODO/FIXME | ✅ 0 |
| pytest collected | ✅ 3845 (6.2s) |
| GitReins tasks | ✅ 5/5 complete, 0 pending |
| CI (gh run list) | ✅ green 5/5 (last 30835624470, T76, 3m11s) |
| Scheduler | ✅ Enabled=true, CooldownS=7200, DecayRate=1, Priority=10, Weight=15 — fleet.toml pin matches, NO PUT |
| Remote | ✅ 0 unpushed, origin/main in sync, 0 incoming, no open issues |
| Deps | ⚠️ outdated non-blocking (standing: aiohttp 3.14.3, anthropic 0.120.2 etc. — no action per standing decision) |

**E2E-001 disposition:** executed at LAST tick of due window (T72–T77).
Next due: T82 (cadence 5–10). Fixture remains pending (recurring).

---

*Generated by Hermes foreman, T77 — 2026-08-03.*
