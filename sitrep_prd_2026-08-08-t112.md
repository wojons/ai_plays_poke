# Sitrep — T112 E2E-001 (luna_v19) — 2026-08-08

## Verdict: E2E executed but DEGRADED — controller OpenRouter key revoked

| Aspect | Result |
|---|---|
| Run | luna_v19_20260808_225436, 80/80 cycles, RUN_EXIT=0, ~90s wall |
| Memory | RSS flat 108→111 MB peak (leak fix HOLDS) |
| Battles | 0 (controller dead — no navigation out of Pallet Town) |
| Screens | overworld (RAM reader), dialog, boot phases |
| Recovery | 11x bounded level-0/1 menu_redraw (tile-locked, controller unable to plan) |
| run_from_battle | 0 (no battles, nothing to flee) |
| Invalid transitions | 0 |
| Frame cache | 259 unique / 639 refs |
| API calls | 73 attempted, ALL failed: `OpenRouter API error 401: User not found` |
| Video | videos/luna_v19_20260808_225436.mp4 (71 KB, 80 frames) |

## Root cause: OPENROUTER_API_KEY revoked

- Key `sk-or-v1-3d144e…bdbeb5` returns **401 "User not found"** on
  `https://openrouter.ai/api/v1/auth/key` — direct curl, twice.
- Same key present in: `ai_plays_poke/.env`, `wojons-mythos/.env`,
  `~/.hermes/.env` (fleet-wide impact — this key was the shared gpt-5.6-luna
  gameplay key).
- 10 OTHER OpenRouter keys on host all return HTTP 200 (valid). So this is a
  key-specific revocation, not an OpenRouter outage.
- `src/core/ai_client.py` loads `.env` via python-dotenv (`load_dotenv`),
  so the project picks up the dead key automatically.

## What was verified vs not

VERIFIED this run:
- Memory-leak fix (74b1f1b) holds — RSS flat 108→111 MB through 80 cycles.
- Intro bypass + boot progression works (title → dialog → name entry → Pallet Town).
- HSM: zero invalid transitions; recovery ladder bounded (no unbounded spam).
- Circuit breaker engaged correctly after 5 consecutive API failures — no
  runaway retry storm (73 calls, then breaker open for remaining cycles).

NOT verified (deferred, key pending):
- Battle-phase gameplay (wild/trainer battles, flee logic, move selection).
- Starter pickup (never reached Oak's Lab conversation flow).

## Actions

- Board task **AP-GAP-006** created: rotate revoked OpenRouter key.
- E2E-001 stays pending; next window T117-T122 (execute once key rotated).
- No code changes this tick — the 401 is external, not a regression.
